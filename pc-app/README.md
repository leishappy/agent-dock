# AgentDock — PC 端

## 功能

- 监听 AI Agent（Claude Code / Cursor）日志，采集任务进度
- 格式化 JSON 通过 USB CDC 串口发给 ESP32
- 系统托盘后台运行

## 环境

```bash
pip install -r requirements.txt
```

## 运行

```bash
python main.py
```
